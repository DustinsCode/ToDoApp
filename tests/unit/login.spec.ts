import Login from '@/components/Authentication/Login.vue'
import { shallowMount, mount } from '@vue/test-utils'

describe("Login.vue", () => {
    
    describe("email field validates", () => {

        it("marks invalid on empty field")
        const wrapper = shallowMount(Login);
    })
})